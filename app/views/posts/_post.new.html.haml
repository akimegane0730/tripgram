.card
  .card-header
    投稿画面
  .card-body
    = form_with model: @post, class: "upload-images" do |f|
      .form-group.row.mt-2
        .col-auto.pr-0
          -# = image_tag avatar_url(current_user), class: "post-profile-icon"
        .col.pl-0
          = f.text_field :caption, class: "form-control-border-0", placeholder: "文章を入力"
      .blank
        🔽      画像選択      🔽
      .mb-3
        = f.fields_for :photos do |i|
          = i.file_field :image
      = f.submit "投稿する", class: "btn-btn-primary"
